overwrite: true
schema: "http://localhost:5001/graphql"
generates:
  src/types/generated.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      scalars:
        DateTime: string
        Upload: File